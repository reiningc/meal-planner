<% include ../partials/header.ejs %>
<div class="ui middle aligned center aligned grid">
    <div class="six wide column">
        <h1 class="ui header">
            <div class="content">
                    Edit <%= plan.name %>
            </div>
        </h1>

        <form class="ui form" action="/plans/<%=plan.id%>?_method=PUT" method="POST">
            <div class="ui stacked segment">
                <div class="field">
                        <input name="plan[name]" value="<%= plan.name %>" type="text">
                    </div>
                <button class="ui fluid large blue submit button" type="submit">Update</button>
            </div>
        </form> 
    </div>
</div>


<a href="/plans/<%= plan.id %>">Go Back</a>

<% include ../partials/footer %>